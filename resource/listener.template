# listener.ora Network Configuration File
# Generated by ChildView v5.0 Platform upgrade
# For Oracle 19.3c installation
# Parameters :
# Oracle Home = [ORACLE_HOME]
# Fully Qualified Host Name = [FQHN]
# Listener Port = [LISTENER_PORT]

SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (SID_NAME = CLRExtProc)
      (ORACLE_HOME = [ORACLE_HOME])
      (PROGRAM = extproc)
      (ENVS = "EXTPROC_DLLS=ONLY:[ORACLE_HOME]\bin\oraclr19.dll")
    )
  )

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = [FQHN])(PORT = [LISTENER_PORT]))
    )
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
    )
  )

ADR_BASE_LISTENER = [ORACLE_HOME]\log
